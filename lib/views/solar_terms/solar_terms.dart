import 'package:flutter/material.dart';

class SolarTermsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('solar_terms')
    );
  }
}
